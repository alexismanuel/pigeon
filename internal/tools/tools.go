package tools

// Package tools will host built-in tool implementations in M2:
// - read(path, offset?, limit?)
// - write(path, content)
// - edit(path, oldText, newText)
// - bash(command, timeout?)
